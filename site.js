const menu=document.querySelector('.menu');const nav=document.querySelector('.navlinks');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open)});nav?.addEventListener('click',e=>{if(e.target.closest('a')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}});const toggle=document.querySelector('.guide-toggle'),panel=document.querySelector('.guide-panel');function closeGuide(){panel.hidden=true;toggle.setAttribute('aria-expanded','false');toggle.focus()}toggle?.addEventListener('click',()=>{panel.hidden=!panel.hidden;toggle.setAttribute('aria-expanded',String(!panel.hidden));if(!panel.hidden)panel.querySelector('button').focus()});document.querySelector('.guide-close')?.addEventListener('click',closeGuide);document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(panel&&!panel.hidden)closeGuide();nav?.classList.remove('open');menu?.setAttribute('aria-expanded','false')}});const form=document.querySelector('form[name="contact"]');if(form){const topic=new URLSearchParams(location.search).get('topic');if(topic&&Array.from(form.elements.topic.options).some(o=>o.value===topic))form.elements.topic.value=topic;form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button[type="submit"]'),status=document.getElementById('contact-status');if(button.disabled)return;button.disabled=true;button.textContent='Sending…';status.hidden=true;try{const response=await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(new FormData(form)).toString()});if(!response.ok)throw new Error('Submission failed');status.textContent='Thank you. Your request has been received. Dean will follow up by email.';form.reset()}catch{status.textContent='Your request could not be sent. Please try again or email ';const a=document.createElement('a');a.href='mailto:contact@northridgeadvisory.net';a.textContent='contact@northridgeadvisory.net';status.appendChild(a);status.appendChild(document.createTextNode('. Your entries have been kept.'))}finally{status.hidden=false;button.disabled=false;button.textContent='Contact Dean'}})}
